/// [MonsterTargetMode] describes the current aggro target of a monster.
enum MonsterTargetMode {
  /// The monster is heading toward the nexus (default behaviour).
  nexus,

  /// The monster is focused on the player character.
  player,
}
